I"�<h3 id="两数之和">两数之和</h3>

<p>给定一个整数数组 nums和一个目标值 target，请你在该数组中找出和为目标值的那两个整数，并返回他们的数组下标。</p>

<p>你可以假设每种输入只会对应一个答案。但是，数组中同一个元素不能使用两遍。</p>

<p>示例：</p>
<blockquote>
  <p>给定 nums = [2, 7, 11, 15], target = 9  <br />
因为 nums[0] + nums[1] = 2 + 7 = 9  <br />
所以返回 [0, 1]</p>
</blockquote>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code><table class="rouge-table"><tbody><tr><td class="rouge-gutter gl"><pre class="lineno">1
2
3
4
5
6
7
8
9
</pre></td><td class="rouge-code"><pre>class Solution:
    def twoSum(self, nums: List[int], target: int) -&gt; List[int]:
        valuedict={}
        for index,num in enumerate(nums):
            ano_num=target-num
            if ano_num in valuedict:
                return [valuedict[ano_num],index]
            valuedict[num]=index
        return None
</pre></td></tr></tbody></table></code></pre></div></div>

<p>tips：将遍历过的元素存到字典中，就可以通过后一个元素与字典中的相加得到结果。</p>
:ET